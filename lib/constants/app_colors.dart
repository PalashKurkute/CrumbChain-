import 'package:flutter/material.dart';

/// Color palette for CrumbChain app
/// Representing sustainability, warmth, and natural connection
class AppColors {
  // Primary Color - Forest Green
  // Symbolizes growth, sustainability, and trust
  static const Color primary = Color(0xFF2E553B);

  // Secondary Color - Terracotta Red
  // Represents warmth, nourishment, and human connection
  static const Color secondary = Color(0xFFC45532);

  // Accent 1 - Golden Wheat
  // Evokes freshness and natural harvest
  static const Color accent1 = Color(0xFFE6A245);

  // Accent 2 - Cream Beige
  // Provides a soft, natural background tone
  static const Color accent2 = Color(0xFFF5E6CE);

  // Neutral Base - Deep Brown
  // Adds grounding and balance, usable for typography or shadows
  static const Color neutralBase = Color(0xFF5A3A1B);

  // Additional utility colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Lighter variants for backgrounds and hover states
  static const Color primaryLight = Color(0xFF4A7B5A);
  static const Color secondaryLight = Color(0xFFD97151);

  // Text colors
  static const Color textPrimary = neutralBase;
  static const Color textSecondary = Color(0xFF8B6D4F);
  static const Color textLight = Color(0xFFFFFFFF);
}
